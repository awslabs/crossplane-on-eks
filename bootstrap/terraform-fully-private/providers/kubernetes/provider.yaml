apiVersion: pkg.crossplane.io/v1
kind: Provider
metadata:
  name: ${kubernetes-provider-name}
spec:
  package: ${ecr_aws_account_id}.dkr.ecr.${ecr_aws_region}.amazonaws.com/crossplane-contrib/provider-kubernetes:${version}
  runtimeConfigRef:
    name: ${runtime-config}
