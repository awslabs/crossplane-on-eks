apiVersion: pkg.crossplane.io/v1
kind: Provider
metadata:
  name: ${helm-provider-name}
spec:
  package: xpkg.upbound.io/crossplane-contrib/provider-helm:${version}
  runtimeConfigRef:
    name: ${runtime-config}
